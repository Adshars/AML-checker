import { Container } from 'react-bootstrap';

const UsersPage = () => {
  return (
    <Container className="mt-4">
      <h2>User Management</h2>
      <p className="text-muted">This page will display user management panel (admin only).</p>
    </Container>
  );
};

export default UsersPage;
